# 줄바꿈 문자를 리포지토리 안에서는 항상 LF로 통일한다. 그동안 core.autocrlf 설정이나
# .gitattributes가 없어서, 파일을 어느 컴퓨터에서 저장하느냐에 따라 CRLF/LF가 계속
# 왔다갔다 했다 — 그래서 실제 내용은 안 바뀌었는데도 git diff/git blame에 파일 전체가
# 바뀐 것처럼 나오는 문제가 있었다. 이 설정 이후로는 커밋할 때 자동으로 LF로 저장되고,
# 체크아웃할 때는 각자 OS 기본 방식으로 풀어준다.
* text=auto eol=lf
